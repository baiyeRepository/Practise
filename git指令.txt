选项1：更新现有 origin 指向新仓库（推荐）
bash

# 将现有的 origin 指向新的 Practise 仓库
git remote set-url origin https://github.com/baiyeRepository/Practise.git

# 验证是否更新成功
git remote -v

#初始化git仓库
git init

#添加所有项目文件
git add

#提交更改
git commit -m "Initial project commit"